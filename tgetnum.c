// int tgetnum(const char *id);
// id: идентификатор параметра, который вы хотите извлечь.
// Возвращаемое значение:

// Если параметр успешно найден, и его значение можно преобразовать в целое число, то tgetnum возвращает это число.
// Если параметр не найден или его значение не может быть преобразовано в целое число, то возвращается -1.
#include <curses.h>
#include <stdio.h>

int main() {
    // Извлечение числового значения columns
    int columns = tigetnum("columns");

    if (columns != -1) {
        printf("Количество столбцов: %d\n", columns);
    } else {
        printf("Параметр columns не найден или его значение не является числом.\n");
    }

    return 0;
}
